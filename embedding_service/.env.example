# Embedding Service Configuration

# API Key for authentication (optional but recommended)
# Generate a secure random key: openssl rand -hex 32
EMBEDDING_SERVICE_API_KEY=your-secret-api-key-here

# Port (default: 5000)
PORT=5000
